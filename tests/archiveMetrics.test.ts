import assert from'node:assert/strict';import{it}from'node:test';import{alignArchiveMetrics,parseMetricsCsv,planMetricDays}from'../tools/shared/archiveMetrics.js';
it('archive metrics parses OI and converts taker buy/sell ratio to share',()=>{const x=parseMetricsCsv('create_time,symbol,sum_open_interest,sum_open_interest_value,sum_taker_long_short_vol_ratio\n2026-07-28 00:00:00,BTCUSDT,100,6400000,3\n');assert.equal(x[0]!.timestamp,Date.UTC(2026,6,28));assert.equal(x[0]!.oi,100);assert.equal(x[0]!.takerBuyRatio,.75)});
it('archive metrics plans UTC daily files without a nonexistent monthly path',()=>assert.deepEqual(planMetricDays(Date.UTC(2026,6,28,12),Date.UTC(2026,6,30)),['2026-07-28','2026-07-29']));
it('archive metrics alignment is causal and expires stale points',()=>{const p=[{timestamp:300_000,oi:10,oiValue:null,takerBuyRatio:.6},{timestamp:900_000,oi:20,oiValue:null,takerBuyRatio:.4}],c=[0,300_000,600_000,900_000,1_800_000].map(timestamp=>({timestamp,open:1,high:1,low:1,close:1,volume:1}));const a=alignArchiveMetrics(p,c);assert.deepEqual(a.oi,[null,10,10,20,null]);assert.deepEqual(a.takerBuyRatio,[null,.6,.6,.4,null]);assert.equal(a.covered,3)});
