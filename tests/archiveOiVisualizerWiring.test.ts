import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{it}from'node:test';it('visualizer enriches heatmap with causal archive OI and keeps fail-soft fallback',()=>{const s=readFileSync('tools/visualizer/server.ts','utf8');assert.ok(s.includes('withArchiveHeatmapAux'));assert.ok(s.includes('alignArchiveMetrics(metrics, candles)'));assert.ok(s.includes('live.oi[i] ?? v'));assert.ok(s.includes('fail-soft, keep API/volume fallback'));assert.ok(s.indexOf('withArchiveHeatmapAux(symbol')<s.indexOf('detectLiquidityHeatmap(snapshot.candles'))});
