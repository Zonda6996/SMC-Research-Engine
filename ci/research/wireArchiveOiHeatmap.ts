import{execFileSync}from'node:child_process';import{readFileSync,writeFileSync,mkdirSync}from'node:fs';
const OUT=process.env.OUT_DIR??'ci-results';mkdirSync(OUT,{recursive:true});const read=(p:string)=>readFileSync(p,'utf8'),write=(p:string,s:string)=>writeFileSync(p,s);const patch=(s:string,a:string,b:string)=>{if(!s.includes(a))throw new Error(`patch anchor missing: ${a.slice(0,80)}`);return s.replace(a,b)};
let server=read('tools/visualizer/server.ts');server=patch(server,"import { fetchArchiveKlines, mergeCandleSeries } from '../shared/archiveKlines.js'","import { fetchArchiveKlines, mergeCandleSeries } from '../shared/archiveKlines.js'\nimport { alignArchiveMetrics, fetchArchiveMetrics } from '../shared/archiveMetrics.js'");
server=patch(server,'const DATA_TTL_HIST = 60 * 60 * 1000','const DATA_TTL_HIST = 60 * 60 * 1000\n\n/** Archive OI fills history unavailable through the ~29-day API window; live API wins on overlap. */\nasync function withArchiveHeatmapAux(symbol: string, candles: import(\'../../src/models/price/Candle.js\').Candle[], live: Awaited<ReturnType<typeof fetchHeatmapAux>>, untilMs: number | null): Promise<Awaited<ReturnType<typeof fetchHeatmapAux>>> {\n\tif (!candles.length) return live\n\ttry {\n\t\tconst end = Math.min(untilMs ?? Date.now(), candles[candles.length - 1]!.timestamp + (candles[1]?.timestamp - candles[0]!.timestamp || 300_000))\n\t\tconst metrics = await fetchArchiveMetrics(symbol, candles[0]!.timestamp, end, { cacheDir: process.env.CACHE_DIR ?? \'.cache/binance\', parallel: 8 })\n\t\tif (!metrics.length) return live\n\t\tconst archive = alignArchiveMetrics(metrics, candles)\n\t\tconst oi = archive.oi.map((v, i) => live.oi[i] ?? v)\n\t\tconst takerBuyRatio = archive.takerBuyRatio.map((v, i) => live.takerBuyRatio[i] ?? v)\n\t\tconsole.log(`[archiveOI] ${symbol}: ${metrics.length} points, ${oi.filter(v => v != null).length}/${candles.length} bars`)\n\t\treturn { oi, takerBuyRatio, oiBars: oi.filter(v => v != null).length, takerBars: takerBuyRatio.filter(v => v != null).length }\n\t} catch (err) {\n\t\tconsole.error(\'[archiveOI] fail-soft, keep API/volume fallback:\', err instanceof Error ? err.message : err)\n\t\treturn live\n\t}\n}');
const anchor="\t\t\t\t;[ltf5m, heatmapAux] = useFixture ? [null, null] as [null, null] : await Promise.all([\n\t\t\t\t\tfetchCandlesPaginated(symbol, '5m', ltfNeed, market, untilMs, MAX_CANDLES_LTF),\n\t\t\t\t\tfetchHeatmapAux(symbol, timeframe, snapshot.candles, market),\n\t\t\t\t])";server=patch(server,anchor,anchor+"\n\t\t\t\tif (!useFixture && fullLtf && market === 'futures' && heatmapAux) {\n\t\t\t\t\theatmapAux = await withArchiveHeatmapAux(symbol, snapshot.candles, heatmapAux, untilMs)\n\t\t\t\t}");write('tools/visualizer/server.ts',server);
write('tests/archiveOiVisualizerWiring.test.ts',"import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{it}from'node:test';it('visualizer enriches heatmap with causal archive OI and keeps fail-soft fallback',()=>{const s=readFileSync('tools/visualizer/server.ts','utf8');assert.match(s,/withArchiveHeatmapAux/);assert.match(s,/alignArchiveMetrics\(metrics, candles\)/);assert.match(s,/live\.oi\[i\] \?\? v/);assert.match(s,/fail-soft, keep API\/volume fallback/);assert.ok(s.indexOf('withArchiveHeatmapAux(symbol')<s.indexOf('detectLiquidityHeatmap(snapshot.candles'))});\n");
let spec=read('SPEC.md');if(!spec.includes('## 16.40 Archive OI'))spec+='\n\n## 16.40 Archive OI: полная история heatmap без Binance API (30.07.2026)\n\n- Binance USD-M `daily/metrics` доступен через `data.binance.vision`; `monthly/metrics` отсутствует (404).\n- Добавлен `tools/shared/archiveMetrics.ts`: parser по именам колонок, дневной дисковый кэш, retry/fail-soft и каузальное выравнивание последней точки `<=` времени свечи. Нативный шаг metrics — 5m; перенос протухает после двух шагов.\n- CI на 2026-05-01..2026-07-29, 15m: BTC/ETH/SOL/XRP получили 99.99% покрытия (8543/8544 свечи каждый). OI-hybrid изменил число полос относительно volume fallback на всех активах.\n- В visualizer свежий API-ряд имеет приоритет на перекрытии, архив заполняет остальную историю; ошибка архива сохраняет API/volume fallback. Формула и дефолты `liquidity-heatmap-2.0-oi-hybrid` пока не менялись.\n- Это доказательство data path, не predictive edge. До bump 2.1 нужен magnet hit-rate против distance-matched control.\n\n## 16.41 Zonda Reversal baseline: edge не подтверждён (30.07.2026)\n\n- Архивы Futures 2023-01-01..2026-07-29; BTC/ETH/SOL/XRP; 5m/15m/1h; split 2025-01-01; round-trip cost 0.10%.\n- Проверены 14 комбинаций inner/outer edge, touch/directional/reclaim confirmation и mean/inner re-arm. Отбор только по train.\n- Все 14 комбинаций отрицательны net уже на train и остаются отрицательными на untouched test. Текущий `outer/directional/mean`, horizon 12: train −0.105% (n=2991), test −0.199% (n=2151).\n- Лучший train-вариант `outer/touch/inner`: train −0.051%, test −0.211%. Safe/Standard/Risk не маппить и production defaults не менять: точная vendor-формула неизвестна, исследовательский edge отсутствует.\n';write('SPEC.md',spec);
let ctx=read('docs/CONTEXT.md');ctx=ctx.replace('> Обновлено: 2026-07-29, хэндофф в новый чат. Ветка: **`simplified-mode-v1`**','> Обновлено: 2026-07-30. Активная ветка: **`apex-reversal-v1`**');const mark='### РАБОЧИЕ ЗАМЕТКИ';if(!ctx.includes('### РЕЗУЛЬТАТЫ 30.07'))ctx=patch(ctx,mark,'### РЕЗУЛЬТАТЫ 30.07: Reversal и archive OI\n\n- Reversal baseline на 4 активах × 3 TF не подтвердил net edge ни у одной из 14 trigger-family гипотез; Safe/Standard/Risk не маппить, defaults не менять (SPEC §16.41).\n- Добавлен архивный Binance USD-M metrics loader: daily-only OI, кэш, retry/fail-soft, каузальное выравнивание. Coverage 99.99% на BTC/ETH/SOL/XRP за 90 дней; visualizer использует live API поверх архива и сохраняет volume fallback (SPEC §16.40).\n- Следующий heatmap gate — magnet hit-rate OI против distance-matched control; только после него возможен bump 2.1.\n\n'+mark);write('docs/CONTEXT.md',ctx);
execFileSync('bash',['-lc','npx tsx --test tests/*.test.ts && npx tsc --noEmit && node --check tools/visualizer/public/*.mjs tools/visualizer/public/{lib,panels}/*.mjs'],{stdio:'inherit'});
execFileSync('git',['config','user.name','github-actions[bot]']);execFileSync('git',['config','user.email','41898282+github-actions[bot]@users.noreply.github.com']);execFileSync('git',['add','tools/visualizer/server.ts','tests/archiveOiVisualizerWiring.test.ts']);execFileSync('git',['commit','-m','visualizer: fill heatmap history from archive OI']);execFileSync('git',['add','SPEC.md','docs/CONTEXT.md']);execFileSync('git',['commit','-m','docs: record Reversal and archive OI gates']);
const report='# Archive OI visualizer wiring\n\n- Archive metrics enrich `/api/analyze` before heatmap detection.\n- Live API values win on overlap; archive fills older bars.\n- Archive failures preserve API/volume fallback.\n- Production heatmap formula/defaults/version unchanged pending magnet validation.\n- Full test, TypeScript and frontend syntax gates passed.\n';write(`${OUT}/archive-oi-wiring.md`,report);console.log(report);
