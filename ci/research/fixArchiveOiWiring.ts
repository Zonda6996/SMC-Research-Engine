import{readFileSync,writeFileSync}from'node:fs';const p='ci/research/wireArchiveOiHeatmap.ts',s=readFileSync(p,'utf8');const old="assert.match(s,/withArchiveHeatmapAux/);assert.match(s,/alignArchiveMetrics\\(metrics, candles\\)/);assert.match(s,/live\\.oi\\[i\\] \\?\\? v/);assert.match(s,/fail-soft, keep API\\/volume fallback/);";const neu="assert.ok(s.includes('withArchiveHeatmapAux'));assert.ok(s.includes('alignArchiveMetrics(metrics, candles)'));assert.ok(s.includes('live.oi[i] ?? v'));assert.ok(s.includes('fail-soft, keep API/volume fallback'));";if(!s.includes(old))throw new Error('test patch anchor missing');writeFileSync(p,s.replace(old,neu));await import('./wireArchiveOiHeatmap.js');
