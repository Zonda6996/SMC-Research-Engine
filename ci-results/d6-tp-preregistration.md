# D6-tp — preregistration (FROZEN): фиксированные тейки по R против контроля

> Заморожено до acquisition новых символов и просмотра любых их исходов. Запрос автора: тейки
> вместо чисто временного выхода («1 к 1 фулл фикс» и выше) + статистика достижения уровней R.
> Конфигурация события ИДЕНТИЧНА d6-mgmt (ΔOI_8h≤−15% И ΔP_8h≤−3%, LONG next-open, gap 8,
> структурный стоп flushLow−0.5×ATR200, консервативный порядок: СТОП ПЕРВЫМ).

## 1. Вселенная

Следующие **12** symbol-fresh перпов: walk-down по `data/d6-mgmt/universe-ranking.json`
(SHA-256 `77f9ee76e0d951b205934f33e99e7e77ddc9a1303b00e2d4c1a41a9a2841b650`, топ-80),
пропуская все символы, уже использованные линией (touched-список + 25 корпуса + 12 из
d6-mgmt, манифест `5ed29eb914d138349040de555ee7ef4560f3107dd2a9cded21eef243f9cb50d6`),
с тем же candle-QA (≥20k баров и пр.). Свечи до cutoff 2026-08-22T00:00:00Z; funding —
официальный REST как ранее.

## 2. Определения

- `riskDist = entryOpen − stopLevel`; `TP(k) = entryOpen + k × riskDist`.
- Внутри бара: сначала проверка стопа (low ≤ stop → выход ПО ЦЕНЕ СТОПА), затем тейк
  (high ≥ TP(k) → выход ПО ЦЕНЕ TP, лимитное допущение), затем таймаут.
- Таймаут всех рук: close бара entry+71 (72 часа).

## 3. Руки (четыре co-primary, объявлены полностью)

| # | имя | тейк | стоп | таймаут |
|---|---|---|---|---|
| 1 | `C-H72` (контроль) | нет | структурный | 72ч |
| 2 | `TP1R-fullfix` | +1×riskDist | структурный | 72ч |
| 3 | `TP2R` | +2×riskDist | структурный | 72ч |
| 4 | `TP3R` | +3×riskDist | структурный | 72ч |

Все сделки net % fixed-notional (5 bps/side + фактический funding за фактическое удержание);
gross@0 дескриптивно. Сделки без полного горизонта исключаются механически со счётом.

## 4. Диагностика MFE (на КОНТРОЛЬНОЙ руке, объявлена)

Доля сделок, чей максимальный благоприятный ход (high от entryOpen) достигает порога
≥1R / ≥1.5R / ≥2R / ≥3R до выхода. Это ответ на вопрос автора «сколько процентов доходит
до 1.5R»; диагностическая метрика, на гейты не влияет.

## 5. Гейты и правило лучшей руки

- Power gate: суммарно ≥100 событий, иначе `INCONCLUSIVE DATA`.
- Каждая рука: pooled mean net%, UTC-day cluster bootstrap CI95 (10k, seed **25082026**);
  GO_arm ⇔ lower95 > 0; breadth положительных символов отчётывается.
- **Лучшая рука**: среди GO-рук с breadth ≥9/12 — максимальный CI-low (как в d6-mgmt §4).

## 6. Терминальность

Reveal один раз; эти 12 символов сожжены для D6-класса. Частичные выходы (split-тейки)
— отдельная будущая preregistration (запрос автора), только на новых данных.
