# Контекст проекта SMC Research Engine для нового ИИ

> Обновлено: 2026-07-24 (после v2.3 и редизайна визуализатора по шестому QA). Ветка: `liquidity-improvements-v1`, HEAD — коммиты 8-го QA («conf v1.7» + «viz: кэш/селектор/причины zone-ended») поверх `c35fb73`. Актуальный фронт работ — раздел 0.5, читать его ПЕРВЫМ; правила движков — SPEC §16.8–§16.17.
>
> Это файл быстрого восстановления контекста, **не замена `SPEC.md`**. `SPEC.md` — главный источник истории решений, результатов и отрицательного знания. Код, особенно `src/strategy/battleConfig.ts`, — источник истины о текущем боевом поведении.

## 0. Обязательный протокол нового чата

Перед любыми идеями, выводами или изменениями кода:

1. Открой актуальный репозиторий и выполни `git pull`/`git fetch`.
2. Прочитай **`SPEC.md` полностью**, от начала до конца. Не ограничивайся последними разделами: старые волны объясняют, что уже тестировалось и почему было отклонено.
3. Прочитай этот файл полностью.
4. Просмотри все tracked-файлы проекта как минимум через `git ls-files`; бинарные результаты можно инвентаризировать, но весь исходный код, тесты, конфиги и Markdown нужно прочитать.
5. Особенно внимательно прочитай:
   - `src/strategy/battleConfig.ts`;
   - `src/core/analysis/runAnalysis.ts`;
   - `src/core/fib/FibLifecycleEngine.ts`;
   - `src/core/analysis/entryModels.ts`;
   - `src/core/analysis/takeLadders.ts`;
   - `src/core/analysis/portfolioBacktest.ts`;
   - `src/core/analysis/regimeFilter.ts`;
   - `src/core/analysis/dedupFilter.ts`;
   - `tools/batch/runBatch.ts`;
   - `tools/forward/forwardRunner.ts`;
   - все тесты в `tests/`.
6. Посмотри последние коммиты и diff, а не исходи из того, что этот контекст всё ещё свежий.
7. Перед утверждением «работает» запусти:

```bash
npm install --package-lock=false --ignore-scripts --no-audit --no-fund
npm test
npx tsc --noEmit
```

На коммите `864895e` проходили 217/217 тестов. На текущей ветке (24.07.2026, после conf v1.7) — 287/287 (`npx tsx --test tests/*.test.ts`), `tsc --noEmit` чистый, `node --check` всех фронтовых модулей чистый (`tools/visualizer/public/**/*.mjs` + легаси-стаб app.js).

8. Не исправляй найденные логические проблемы молча. Сначала покажи проблему, экономический эффект, варианты решения и тест, который отличит варианты.
9. Не предлагай повторно уже убитые идеи без нового information set и явного объяснения, почему теперь результат может измениться.
10. Общайся с пользователем по-русски, прямо, плотно, с цифрами. Не выдумывай результаты и параметры.

---

## 0.5 Текущий фронт работ (24.07.2026): QA v2.3 и визуализатора 2.0 по скринам

Активная работа — liquidity-ветка (SPEC §14, §16.x). Canon/battle-пайплайн (разделы 2–8) — фоновый контекст, не трогать без явной просьбы.

### Состояние ветки (все запушено в liquidity-improvements-v1)

```text
232ab5b  v1.7: единое окно зон, единая последовательность подтверждения, каузальный far (SPEC §16.8)
e0018ee  v1.8: невыметенный якорь, проторговка 4/6 баров, idle-таймаут, минус local-swing, дедуп near (SPEC §16.9)
e009f89  v1.9: ANCHOR_DEEPENED, sweep-continuity, стоп за историей, swept-through, условная отставка, стековый far (SPEC §16.10)
10ac783  v1.10: проторговка после переноса якоря, дедуп по перекрытию 0.6 (SPEC §16.11)
39e50ee  v2.0: liquidity-first зоны от полок heatmap (SPEC §16.12)
7a8fbb7  v2.1: полки из провалов плотности, потолок 8% цены, свежесть 300, гард дедупа 2× (SPEC §16.13)
20a050d  v2.2: родство стеков, сила полки stackShare + фильтр UI, перф-фиксы (SPEC §16.14)
60176b5  v2.3 + viz 2.0: замещение при удержании массы; полный редизайн фронта (SPEC §16.15)
2d0dbb4  v2.4: stack-consumed на закрытии бара снятия (SPEC §16.16)
c35fb73  viz: полировка 7-го QA — палитра мышью, heatmap вне 15m, зум без сбросов, справки свёрнуты (SPEC §16.16)
поверх   conf v1.7: weaknessFailLimit=3 + конфиг-параметр + сетка на полной 15m-истории; viz: кэш данных (пересчёт за секунды), полный список монет, «чем кончилась зона» при zone-ended, конфиг подтверждения в UI (SPEC §16.17)
```

Версии движков: `liquidity-poi-2.4-consumed-at-close`, `poi-confirmation-1.7-weakness-limit`. Тесты 287/287, `tsc --noEmit` чистый, `node --check` всех фронтовых модулей чистый. Правила движков — SPEC §16.8–§16.17 поверх §12–§15.

### Визуализатор 2.0 (редизайн, отгружен — SPEC §16.15)

Фронт переписан на модули: `index.html` + `styles.css` (дизайн-токены shadcn/Vercel, Geist) + `app.mjs` + `lib/{state,format,chart,api,palette}.mjs` + `panels/{stats,heatmap,zones,confirmation,lab,config}.mjs`; старый `app.js` — стаб. Зоны — прямоугольниками (custom primitive, вне автошкалы), hover-карточки, клик-фокус; панель «Настройки движков» (сервер принимает poiConfig/hmConfig, whitelist числовых ключей, дефолты не мутируют); «Мои зоны» (ручная разметка, localStorage, экспорт); командная палитра ⌘K; воронка в шапке; пресеты; Decision Lab перенесён без изменений логики и ключа localStorage. QA по скринам пользователя — следующим шагом; вероятные полировки: типографика/отступы, поведение фокуса, состав команд палитры.

### История калибровки (кратко; детали и цифры — SPEC §16.13–§16.15)

- **v2.3 (6-й QA):** кейс ETH «нет зоны над полкой 2030» — сползшее вверх свежее поколение отставляло старшую и сиротило массу. Замещение теперь только при УДЕРЖАНИИ ≥50% старшего стека; сползшие окна — дубли. Ручная карта BTC пользователя совпала с движковой почти 1-в-1; последовательные зоны SOL — сегменты непрерывной лестницы (не дефект).

- **v2.1 (4-й QA):** гиганты убиты — полки = супер-цепи, разрезанные по ПРОВАЛАМ ПЛОТНОСТИ (корзины 0.4% цены, провал ≥3 корзин < 25% пика); потолок 8% от края (ATR-потолок удалён); свежесть 300; minShare 0.03. Кластеризация по ядрам отвергнута данными.
- **v2.2 (5-й QA):** РОДСТВО СТЕКОВ (stackKinshipShare 0.5) — «близнецы» сползших окон (71% общих пулов при перекрытии цен 50%) и вложенные поколения роста схлопнуты: тронутая старшая главнее (младшая дубль), нетронутая отставляется свежей геометрией (supersededAt). Сила полки stackNotional/stackShare + фильтр UI «Стек ≥ 10% сильнейшего» (дефолт; мизеры уходят, правило рождения НЕ трогали — пик-фильтр рождения и реестр пулов на всю сторону проверены и отвергнуты, SPEC §16.14). UI: нефокусные зоны вне автошкалы (лечит «график пропадает» на «любом статусе»), кэш перезаливки свечей.
- Пороги провалов (0.25/3) проверены чувствительностью (0.3, 0.35, minBins 2): каждый вариант ломает лонги сильнее, чем чинит ширину ближнего шорта; ручки уходят в UI-конфиг редизайна — подбирать на данных пользователя (у него OI-гибрид, у песочницы volume-proxy).

### ГЛАВНОЕ ЗАДАНИЕ №1: редизайн визуализатора (утверждён вариант «ванильная дизайн-система»)

Решение пользователя (24.07): ванильный вариант БЕЗ сборки, НО с чистой архитектурой — «чтобы по коду и структуре всё аккуратно было, а не inline-стили как сейчас», стиль shadcn/vercel, хочет «вау»: приятно проводить время, фичи, красиво, стильно. План реализации:
- Структура: index.html (семантическая разметка) + styles.css (дизайн-токены: палитра/типографика/радиусы/тени в духе shadcn dark) + JS-модули `public/*.mjs` (`node --check` работает с .mjs, MIME в server.ts уже есть) — никаких inline-стилей и одного файла-простыни.
- Фичи: зоны ПРЯМОУГОЛЬНИКАМИ с заливкой и градацией по силе стека (custom primitive lightweight-charts v5), hover-карточки зон, командная палитра Cmd+K (загрузка/панели/поиск зоны), панель конфигов движков с «Пересчитать» (бэкенд готов: LiquidityPoiContext.config; для heatmap — аргумент detectLiquidityHeatmap; серверу добавить приём JSON-оверрайдов в /api/analyze), пресеты вьюера, статус-бар воронки (зоны→попытки→входы), горячие клавиши, плавные переходы/скелетоны.
- Сохранить обязательно: запуск `npx tsx tools/visualizer/server.ts` (порт 7788), без автозагрузки (кнопка «Загрузить»), панели Heatmap / Liquidity POI / POI Confirmation / Decision Lab, кнопка «Зоны на 4h», русские подписи и пояснения терминов, «склонировал и запустил».

### ГЛАВНОЕ ЗАДАНИЕ №2: параллельно — визуальное QA v2.2 по скринам пользователя

Пользователь смотрит 4h limit 5000 (или 25k) и присылает скрины; каждый кейс воспроизводить на данных до правок. Открытое из 5-го QA: ширина ближнего шорта (у пользователя ~67.5–70, у движка свежайшее поколение шире — на его OI-данных проверить; если мешает — крутить shelfValleyShare/MinBins в UI-конфиге редизайна); нижняя граница ближней зоны у цены (пулы копятся у цены и расширяют свежие поколения вниз).

### ROADMAP (после редизайна): МТФ-подтверждение и упрощённый режим

Метод пользователя — связки ТФ (контекст → подтверждение уточнённо/упрощённо): 1W→1D/4h, 1D→4h/1h, **4h→1h/15m (сейчас реализована)**, 1h→15m/5m. «При уточнённой работе можно меньше фильтров контекста; чем больше фильтров — тем выше вероятность отработки даже от упрощённой». Упрощённое подтверждение (вырезка из метода): (1) перепроданность/перекупленность по индикатору GGI ZONE на 1h при сильном тренде / 4h в боковике; (2) вход от возобновления движения — ЗАКРЫТАЯ свеча в сторону зоны на 1h (тренд) / 4h (боковик); (3) риск 1–3% (редко 5%) на все открытые, ≤2–3 сделок в день, контртренд — меньший объём; (4) на альтах +6–8% чистого хода — стоп в бу и/или частичная фиксация 25–50%, полная по целям или на 15–20%; (5) доборы только после бу предыдущих; (6) изолированная маржа, плечо ≤×10 (волатильные ≤×5); (7) стоп = ликвидация −1% (≈9% чистого хода). Реализация потребует: параметризацию ТФ подтверждения (частично готово — конфиги прокинуты), режим «упрощённого» подтверждения как альтернативной машины состояний, отметку тренд/боковик. Это же снимает потолок «28 попыток» (15m-история ограничена MAX_CANDLES_LTF 60k×5m ≈ 208 дней — старые зоны без попыток; на 1h окно в 4 раза глубже).

### Исходное ТЗ v2.0 (утверждено, SPEC §16.12)

Пользователь трижды упёрся в один дефект: зоны рождаются от СТРУКТУРЫ (protected/EQ/outer), а его метод строит их от ЖИРНЫХ ПОЛОК heatmap. Его эталонная карта — скрины третьего QA: 5 зон, все = полка + ближайший экстремум. Симптомы старой схемы: «важные» шорты на 84.6k/97.9k/116.4k (древние нетронутые вершины), зоны «в воздухе» (far от пулов, невидимых в его вьюере с age-фильтром 500 баров), ближайший лонг 63602→62873 не накрывает полку НАД near. Дизайн v2.0 — SPEC §16.12, реализация начата/продолжается. Подтверждение (1.6) НЕ меняется — оно зона-агностично.

### ГЛАВНОЕ ЗАДАНИЕ №2: полный редизайн визуализатора (утверждено)

Стиль shadcn/ui + Vercel: строгий, минималистичный, аккуратный; их компоненты МОЖНО использовать. Больше настроек и кастомизации (конфиги движков в UI). Сохранить обязательно: запуск `npx tsx tools/visualizer/server.ts` (порт 7788), без автозагрузки (кнопка «Загрузить»), панели Heatmap / Liquidity POI / POI Confirmation / Decision Lab, кнопка «Зоны на 4h», русские подписи и пояснения терминов, `node --check` для JS фронта. Если вводится сборка (React/Tailwind) — не сломать «клонировал и запустил» (обсудить с пользователем no-build вариант на CDN или закоммиченный dist).

### Решения пользователя (сводка, детали в SPEC §16.8–16.12)

1. Последовательность подтверждения одна для всех зон: заход → остановка (лой) → отскок (не участвуем) → пересвип → защита → тест слабости → возобновление → вход.
2. Остановка = направленное закрытие после 4 тихих баров; отскок = ещё 6 баров (медианы были 1 бар — свечной шум).
3. Якорь пересвипа = самый глубокий НЕВЫМЕТЕННЫЙ экстремум зоны; свип его «тратит». Потеря защиты переносит якорь и строит подтверждение ЗАНОВО (v1.10).
4. Защита: свип-свеча ИЛИ следующая закрылась обратно — same-bar защита ОСТАЁТСЯ (подтверждено третьим QA).
5. Тест слабости: объём свечи ВОЗОБНОВЛЕНИЯ выше объёма последней откатной (80/60/40 → 50: 50>40 — вход). Объём на приходе — только пометка arrivalVolumeRatio (×SMA20 по 4h), не фильтр.
6. Вход отменяется при риске > 1.5 ATR15 (ENTRY_CANCELLED, попытка живёт, но тесты/отмены idle-таймер НЕ продлевают).
7. Лимита попыток нет; смерть попытки: 96 баров без структурных событий, пробой зоны закрытиями, конец окна зоны (попытка с состоявшимся пересвипом доигрывается за окном).
8. Зона умирает: 4h close телом за far / фитиль НАСКВОЗЬ за far (swept-through) / отработала / отставка. Consumed (фитиль сквозь near) — информационная пометка.
9. ATR-fallback зоны не торгуются; near-дубли (0.25 ATR или перекрытие ≥0.6 меньшей) подавляются (duplicateOf, геометрия не мутирует).
10. Стоп за историческим экстремумом окна, если тот глубже свип-экстремума в пределах 0.5 ATR.
11. Все константы в LIQUIDITY_POI_CONFIG / POI_CONFIRMATION_CONFIG с русскими комментариями; не менять без согласования. Магические числа пользователя раздражают.
12. (24.07, v2.1) Полки режутся по провалам плотности, НЕ по дистанции ядер (ядерная кластеризация проверена и отвергнута данными — SPEC §16.13). Пакет принят: корзины 0.4%, провал ≥3 корзин < 25% пика, свежесть 300, minShare 0.03.
13. (24.07, v2.1) Потолок высоты зоны — только % от цены края (8%), ATR-нога отклонена: резала эталонные ручные зоны в спокойном режиме.
14. (24.07, v2.1) Дедуп выросших полок — вариант А: перекрытие считается дублем только при сопоставимой высоте (младшая ≤ 2× старшей); вариант «выросшая отставляет старую» отклонён (сброс окон подтверждения — риск воронки §16.8).
15. (24.07, v2.2) Родство стеков (≥50% общего notional меньшего стека = один объект): тронутая старшая главнее (младшая дубль — окно в работе сохраняется), НЕтронутая отставляется свежей геометрией (supersededAt) — уточнение решения 14 без его цены: окна сбрасываются только там, где в них ничего не вложено.
16. (24.07, v2.2) Слабые полки не режутся правилом рождения (два кандидата отвергнуты данными — SPEC §16.14), а скрываются фильтром отображения «Сила стека ≥ 10% сильнейшего» (дефолт; метрика stackShare в кандидате).
17. (24.07) Редизайн: ванильный вариант без сборки, обязательна чистая архитектура (без inline-стилей и файла-простыни), стиль shadcn/vercel, «вау»-планка.

### Открытые вопросы

1. ЗАКРЫТО в v2.0/v2.1: зона 67255→70442 пропадала из-за ran-away — правило удалено, жизнь зоны = жизнь её стека; в карте v2.1 ближайший шорт 67553→70022 живёт после касаний.
2. Кейс «касание снизу» (скрин 7 второго QA) — на моих данных 0/94 нарушений инварианта; ждём детали от пользователя.
3. Гард входа 1.5 ATR: после глубокого свипа валидные сетапы отменяются по риску; альтернатива — лимитка на ретесте. Решить по QA v2.0.
4. Merge-семантика «обе зоны открыты» оценивается на конец загрузки (карта может отличаться день ото дня) — принятый компромисс.
5. Пометка «пришли на объёме» в Telegram-уведомления — хотелка пользователя на будущее.

### Рабочие заметки для нового ИИ (важно)

- Песочница агента: Binance API гео-блокирован; данные брать с data.binance.vision (архивы klines futures/um, monthly+daily CSV, timestamps в ms). npm работает после запроса доступа.
- Пуш в GitHub — через MCP-действие github__push_files (owner Zonda6996, repo SMC-Research-Engine, branch liquidity-improvements-v1); после пуша сверять git fetch + git diff origin (должен быть пуст) и git reset --hard origin/…. Коммиты в стиле «poi v1.x: …». Пользователь разрешил автопуш в рабочую ветку; merge в main — только по явной просьбе.
- Диагностика: одноразовые скрипты в tmp/diag (в репо не пушатся; пуш идёт явным списком файлов через github__push_files). Набор 24.07: buildData.ts (сборка btc-4h.json из CSV data.binance.vision), map.ts (активная карта + кейсы QA), shelfGaps.ts (разрывы ядер полок), profile.ts (ASCII-профиль notional против ручной карты), missing.ts (разбор пропавших эталонных зон + варианты конфига через context.config). Датасет: btc-4h.json = 5000 баров до 22.07.2026 (дневной архив публикуется с задержкой ~сутки). Пользователь грузит 4h с limit 5000 — диагностику гонять на 5000 барах.
- QA-цикл: пользователь смотрит визуализатор по скринам → присылает разметку → каждый кейс ВОСПРОИЗВОДИТЬ на данных до правок → предлагать правила → согласовывать → реализовывать с тестами → SPEC-секция → пуш. Скрины пользователя = ТЗ.
- Стиль: по-русски, плотно, с цифрами; английские термины пояснять в скобках; честный пушбек (автосогласие пользователь считает вредом); версии движков бампать при каждом изменении правил; после правок — полный прогон тестов + tsc + node --check.

---

## 1. Что это за проект

SMC Research Engine — TypeScript/Node.js research-платформа для проверки авторской SMC/Fibonacci-логики на крипто-фьючерсах. Это не классический ICT, не готовый торговый бот и не лицензия придумывать новые правила структуры.

Основной pipeline:

```text
Candles
→ PivotDetector
→ SwingEngine
→ StructureEngine / MarketStructureEngine
→ BosChochEngine
→ FibGridEngine
→ FibLifecycleEngine
→ batch/replay/portfolio/forward tooling
```

Канонический оркестратор — `runAnalysis()` в `src/core/analysis/runAnalysis.ts`. Он чистый: считает snapshot без вывода и сетевого исполнения.

Рынок исследования: Binance USDT-M candles. Пользователь торгует/оценивает исполнение под BingX. Модель costs:

- maker: 0.02%;
- taker: 0.05%;
- дополнительный stop slippage allowance: 0.02%;
- входы и тейки предполагаются maker, стоп и time-stop — taker+slippage.

Исторический основной universe последних исследований:

- 14 активов: BTC, ETH, SOL, XRP, BNB, DOGE, ADA, AVAX, LINK, SUI, TON, NEAR, APT, LTC;
- таймфреймы: 15m, 30m, 1h;
- Binance USDT-M futures;
- H1/H2, asset/TF-разрезы и rolling walk-forward применяются как защита от подгонки.

---

## 2. Текущий боевой source of truth

Источник истины — `BATTLE_CONFIG` в `src/strategy/battleConfig.ts`. Не копируй его числовые параметры в другие модули.

### Canon stream

#### Deep

- направление: по сетке;
- touch entry: 38.2;
- stop: 15;
- full take: 61.8;
- time-stop: нет;
- историческое ожидание после costs: около `+0.358R` на сделку.

#### OTE

- направление: по сетке;
- touch entry: 78.6;
- stop: 61.8;
- full take: 100;
- time-stop: 20 баров;
- историческое ожидание после costs: около `+0.244R` на сделку.

Для canon включён bigbar-фильтр, но его исполнимость для resting touch-limit сейчас является открытым критическим вопросом — см. раздел 7.

### Canon sizing

`canonRiskMultiplier()` перемножает:

- freshness: `≤3 → 2.0`, `4–15 → 1.0`, `16+ → 0.5`;
- swing compactness относительно rolling median: compact `1.4`, wide `0.7`;
- session 15–20 UTC `1.2`, но session layer сейчас выключен.

Исторический research-результат sizing stack: около `0.280 → 0.362 R/unit`, то есть +29% к качеству аллокации. Это не означает, что production budget normalization уже корректно реализована.

### Reverse stream после исправления SPEC 7.45

Текущий reverse — **только mirror**:

- активируется после canon OTE entry;
- направление против сетки;
- entry 100;
- stop 120;
- take 78.6;
- cancelBeyond 0;
- честное ожидание: `+0.172R`, WR 60.5%, H1 `+0.169`, H2 `+0.175`, n=3515.

`fade141` удалён из `BATTLE_CONFIG` и новых forward-сигналов.

### Reverse sizing

`reverseRiskMultiplier()` использует свежесть canon-касания:

- `≤3 → 1.5`;
- `4–15 → 1.0`;
- `16+ → 0.7`.

На исправленном mirror-only пуле avgR по этим бакетам: `0.268 / 0.178 / 0.138`. Compactness для reverse не прошла и не используется.

---

## 3. Важнейшее исправление SPEC 7.45: fade141 был look-ahead

Ранее reverse состоял из `mirror@100` и `fade141@141` с first-fill-wins и показывал `+0.347R`.

Проблема: fade оценивался на OTE-сетках, где canon **впоследствии вошёл**, хотя fade-заявка стартовала при создании сетки, когда будущий canon entry ещё неизвестен. Это selection look-ahead.

Unconditional проверка дала:

- все OTE-сетки: n=5625, avgR `−0.130`;
- canon вошёл: n=2983, avgR `+0.205`;
- canon не вошёл: n=2642, avgR `−0.508`, WR 19.2%;
- отрицательный результат устойчив на H1/H2.

После переноса fade activation на момент после canon OTE entry выяснилось:

- цена не может дойти от 78.6 до 141, не пройдя 100;
- mirror на 100 всегда заполняется раньше;
- fade-only n=0;
- first-fill-wins вырождается в mirror-only.

Поэтому fade удалён, старые `+0.347R` признаны завышенными. Новый ИИ не должен ссылаться на `0.347` как на актуальное ожидание reverse, даже если эта цифра осталась в более раннем разделе `SPEC.md` или legacy journal report.

Методологическое правило, добавленное после этой находки:

> Любое условие отбора universe должно быть проверяемо в момент постановки заявки. Если eligibility зависит от будущего входа/исхода другой leg, это look-ahead.

---

## 4. Forward runner после последних изменений

Файл: `tools/forward/forwardRunner.ts`.

Архитектура: stateless replay последних 3000 свечей на каждом `symbol|tf`, состояние дедупликации в `tmp/forward/state.json`, журнал в `tmp/forward/signals.jsonl`, опциональные Telegram-уведомления.

Добавлены события:

- `setup` — сетка создана, нужно заранее поставить canon limit;
- `cancel` — снять незаполненную заявку;
- `signal` — fill;
- `outcome` — tp/stop/timestop.

`--report` считает статистику только по signal/outcome, но показывает число setup/cancel. Legacy fade141-записи читаются для совместимости, новые не создаются.

Это улучшило старую проблему «сигнал-некролог», когда пользователь узнавал о touch только после закрытия свечи. Но implementation ещё не полностью исполнима — см. открытые вопросы.

---

## 5. Что уже исследовано и закрыто

Не предлагай это как новую идею:

- close/candle confirmations;
- MTF CHoCH confirmation;
- partial exits и runners;
- break-even management;
- trailing по уровням;
- re-entry после стопа;
- scale-in/усреднение;
- динамическая переподгонка stop/take cells;
- per-symbol/per-TF cherry-picking;
- HTF trend/alignment filter;
- deep-mirror;
- fade241;
- подход к зоне `approachAtr` и wick fraction;
- equity-streak sizing;
- fixed R:R для OTE;
- дальние цели 141/241 вместо магнитных 61.8/100.

Причины подробно описаны в `SPEC.md`: подтверждения опаздывают, runners разбавляют edge после магнитного уровня, BE/trailing выбивают будущих победителей, re-entry торгует уже сломанный уровень, scale-in получает adverse selection, MTF теряет V-развороты и платит худшей ценой.

### Проверенные и принятые результаты

- full single take лучше partial/BE/runners;
- оптимальные fixed cells: deep `15×61.8`, OTE `61.8×100`;
- OTE entry 78.6 подтверждён полным entry×stop×take sweep;
- freshness и compactness работают как sizing, не hard filters;
- OTE time-stop 20 даёт небольшой плюс;
- rolling walk-forward подтвердил стабильность fixed cells;
- pessimistic intrabar почти не искажает результат для stop/take conflicts;
- volume spike `volRatio≥2` ухудшает OTE, но эффект признан слишком слабым/немонотонным для production layer;
- текущий reverse — только mirror, не fade.

---

## 6. Архитектурный разрыв, который ещё не закрыт

`src/core/analysis/portfolioBacktest.ts` не является portfolio backtest текущего `BATTLE_CONFIG`.

Он исторически собирает lifecycle-сценарии `ote/deep/breaker`, использует одинаковый `riskPct`, `netBeR()` старого lifecycle и tie-break:

```text
entryAt → symbol → timeframe → scenario → id
```

Он не моделирует полноценно:

- новые stop/take cells battleConfig;
- raw/normalized canon sizing;
- mirror activation как parent-child lifecycle;
- reverse sizing;
- единый parent setup risk;
- реальную приоритизацию при max concurrent risk.

Поэтому его equity/DD/Monte Carlo нельзя автоматически называть портфелем текущей canon+mirror системы.

Будущий правильный слой должен строить единый chronological family ledger непосредственно из `BATTLE_CONFIG` и использовать один и тот же replay в batch и forward.

---

## 7. Forward runner v2 (сделано после ревью 864895e)

Текущая версия журнала: `battle-7.45-exec-v2`. Детали и принятые решения — SPEC 7.46.

Исправлено:

- state и события версионированы; старый state не открывается молча;
- FORWARD определяется по заявке/размеру, известным до fill, а не по времени outcome;
- carry-in и catch-up навсегда остаются backfill;
- mutable `swingPool` удалён, median строится по прошлым 200 уникальным сеткам;
- freshness исполняется через `SETUP` и заранее отправленные `AMEND` перед барами 4/16;
- touch fill важнее bigbar свечи касания; post-close bigbar не отменяет уже исполненную лимитку;
- mirror получает setup после OTE fill и торгуется только со следующего бара;
- structural cancel получает реальный confirmIndex противоположного события;
- отчёт показывает clean forward, backfill, pending orders и open trades;
- добавлены тесты median/report/idempotency.

После изменения execution semantics старые цифры `deep 0.358`, `OTE 0.244`, `mirror 0.172` в forward-report — только старые benchmarks. Нужен отдельный batch-пересчёт с теми же исполнимыми правилами.

Перед запуском v2 старую `tmp/forward` обязательно архивировать или удалить.

### Что ещё проверить

1. Запустить полный `--eval-entry`: блок SPEC 7.47 уже сравнивает old/executable bigbar и old/next-bar mirror; fixture прошла, но статистикой не является.
2. По полному TXT+CSV установить новые честные benchmarks deep/OTE/mirror.
3. Добавить более прямой синтетический тест exact cancel index.
4. Построить единый battle-family portfolio ledger; legacy `portfolioBacktest.ts` всё ещё не равен текущему battleConfig.

---

## 8. Приоритет следующей работы

1. Запустить чистый forward v2 после удаления старой папки.
2. Доделать визуализатор так, чтобы он использовал тот же battle execution layer.
3. Пересчитать old-vs-executable bigbar и mirror next-bar.
4. Проверить fade141 только после stop mirror.
5. Затем family sizing/allocator и остальные новые идеи.

---

## 9. Важные команды и файлы результатов

Основные команды:

```bash
npm test
npx tsc --noEmit
npm run batch -- --eval-entry
npm run forward -- --once
npm run forward -- --report
npm run forward -- --fixture
npm run portfolio -- ...
```

Перед реальным длинным прогоном изучить CLI-шапку и `parseArgs()` в `tools/batch/runBatch.ts`.

Forward state/journal:

```text
tmp/forward/state.json
tmp/forward/signals.jsonl
```

Batch results генерируются research runner; не делай вывод по одному summary без CSV-разрезов H1/H2, symbol, TF и проверок одинакового universe.

---

## 10. Стиль работы с пользователем

- Ответы только на русском, если пользователь не попросил иначе.
- Без мотивационной воды.
- Сначала факт из кода/SPEC, затем интерпретация.
- Любую оценку помечать как оценку.
- Если результат плохой — говорить прямо. История fade141 показывает, что пользователь предпочитает честное удаление красивого edge его защите.
- Не считать большой totalR доказательством без проверки universe eligibility на момент решения.
- Не менять архитектурные правила без согласования.
- После любой правки: тест, `tsc`, fixture/synthetic regression, затем описание diff.
- Коммит/пуш — только если пользователь явно попросил или подтвердил.
- Архивы с правками отдавать структурно (пути от корня репо), чтобы файлы можно было просто заменить; к архиву — SHA256 и готовый блок git-команд без пояснений.
- По зонам/логике давать честное трейдерское мнение и пушбек на плохие идеи; автоматическое согласие пользователь считает вредом.

---

## 11. Короткая формула текущего состояния

```text
Боевой кандидат:
canon deep 38.2 → stop15 → take61.8
canon OTE 78.6 → stop61.8 → take100, time-stop20
+ canon freshness×compact sizing
+ mirror reverse 100 → stop120 → take78.6
+ mirror freshness sizing 1.5/1.0/0.7
− fade141 удалён как look-ahead/non-executable

Но перед деньгами ещё проверить:
bigbar executability
risk quantity до fill
causal rolling median
точный cancel timestamp
pre-touch mirror setup
единый battle-family portfolio ledger
```
