# ROADMAP — план активной работы (фильтры / UI / утечки)

> **Что это:** рабочий план текущих линий. Зашёл — видно, что делаем, что сделано, что открыто.
> **Как вести:** статус в чекбоксах (`[ ]` → `[x]`), краткий итог рядом с задачей. Треки можно
> вести параллельно. Живёт до закрытия всех треков → полезные выводы дистиллируем в спеки /
> `NEGATIVE-KNOWLEDGE.md`, затем удаляем этот файл.
> **Как часто меняется:** часто. Читать после `HANDOFF.md`. Язык — русский.

---

## Принципы (из AGENTS.md)
- Правила анализа не выдумываем (§2.1). Пороги/условия значимости — только по явному решению
  автора; в задачах помечено «⚠ решает автор».
- Найденное не чиним молча (§2.2): сначала показать/обсудить, потом менять.
- Дисциплина проверки: гипотеза → отложенное окно (OOS), а не подбор красивой ячейки глазами
  в визуализаторе (это multiple-testing, регрессия к среднему на форварде).

---

## Трек A — корректность движка

- [x] **A1. Развязать число сигналов от режима.** ✅ Реализовано.
  Проблема: safe/risk/standard берут ОДИН и тот же набор кандидатов, но число реальных входов
  разное (ONDO 15m 20k: safe **88** / risk **112** / standard **86** на 1658 кандидатах).
  Причина — кулдаун `blockedUntil = exitIndex + postExitBars` в `src/core/signals/ArrowTradeReplay.ts`:
  у risk стоп ближе → сделки короче → блокировка снимается раньше → влезает больше входов.
  У вендора режим почти не меняет число сигналов (±5-8). Следствие: сравнение safe vs risk
  сейчас НЕ «яблоки к яблокам» (разные N и разные наборы сделок).
  Варианты (⚠ решает автор): (а) единая режимо-независимая сетка допуска сигналов (кулдаун по
  фиксированной структуре/числу баров, не по exit конкретной сделки) → все режимы торгуют один
  набор, различаясь только управлением, как у вендора; (б) оставить, но всегда сравнивать режимы
  на общем наборе. НЕ менять без решения.
  **Диагностика (2026-08-13, ONDO 15m off, Binance-перп, 20k):** разрыв подтверждён как чисто
  кулдаунный — заморозка набора входов на safe роняет risk 112 → 88 (≈ вендор 89). Побочно:
  risk/off на ONDO по факту УБЫТОЧЕН (−4.44R, PF 0.91) — привлекательные risk-цифры были у
  фильтра slope + малая выборка (n=48). Чистого «референсного режима» с равным N нет:
  `replayArrowSignals` всегда доп. применяет свой кулдаун (frozen-on-safe → standard = 54).
  Лечение — РАЗДЕЛИТЬ допуск сигнала (режимо-независимый) и управление (per-mode). ⚠ Правило
  допуска выбирает автор: (а) фикс. спейсинг по барам; (б) допуск на уровне сигнала без
  exit-кулдауна; (в) канонический режим задаёт набор + отключить кулдаун в реплее этого набора.
  Раннер диагностики: `tmp/modeAdmissionProbe.ts`.
  **Резолюция (2026-08-13):** (в)-safe ОТПАЛ — наш safe-кулдаун выше вендора и гуляет по активам
  (BTC 96 / ETH 100 / ONDO 88 против вендора ~83-85). Вместо этого — вариант **(а): фиксированный
  шаг в БАРАХ**. Подтверждено: у вендора число стрелок ~85-90 не зависит ни от актива, ни от ТФ
  (BTC safe: 3m 85 / 15m 85 / 30m 85 / 1h 88 / 2h 90) → шаг именно в барах. `N≈180` баров матчит
  вендора по всем активам и ТФ (свип: `tmp/spacingSweep.ts`). Свип шага по перформансу
  (`tmp/stepPerfSweep.ts`): **шаг НЕ создаёт edge** — BTC/ETH отрицательны при любом N, ONDO слабо
  плюсовой при любом N. Поэтому N выбираем по верности/сравнимости (вендор-якорь 180), а НЕ по
  макс. Net R (это был бы оверфит на шуме ONDO).
  **Реализация A1 (согласовано направление):** режимо-независимый допуск (greedy min-spacing N=180
  баров) → все режимы управляют одним набором стрелок, каждая стрелка = независимая сделка (без
  exit-кулдауна). N — именованная константа (при нужде per-TF). Радиус: `replayArrowSignals` зовут
  бенчмарк/тесты/визуализатор → новый шаг допуска вживляем аккуратно, ядро реплея сохраняем, `tsc`+тесты.
  **Сделано (2026-08-13):** `ARROW_SIGNAL_SPACING_BARS = 180` + `admitArrowSignals()` (greedy min-spacing по
  барам) в `ArrowSignalEngine.ts`; `replayAdmittedArrowSignals()` (каждая стрелка = независимая сделка, без
  exit-кулдауна) в `ArrowTradeReplay.ts` — старый `replayArrowSignals` (per-mode кулдаун) сохранён для
  совместимости и тестов. Вживлено в визуализатор (`tools/visualizer/server.ts`) и бенчмарк
  (`tools/research/auditReversalBenchmark.ts`). `tsc` чист; тесты 503/505 (2 падения — отсутствующий
  `data/vendor-exports/bybit-btcusdt-perp-15m.csv`, не связано с A1). Проверка режимо-стабильности на реальных
  20k барах (`tmp/a1OndoReproduce.ts`): ONDO 15m — OLD safe 88/std 86/risk 112 → NEW 86/86/86; BTC 15m —
  OLD 96/100/121 → NEW 88/88/88 (admitted матчит вендор-плотность ~85-90).

- [x] **A2. Закрыть утечку #2 (`pool.notional` за всю жизнь пула).** ✅ Исправлено (2026-08-13).
  Был блокер доверия к фильтрам liquidity/combo (идут по POI-пути). Решение — вариант B
  (`notional-as-of-t`): движок отдаёт `notionalSchedule` + `notionalAsOf(pool,t)`, POI считает
  отбор/тайминг полок по причинной массе. `tsc` чист, тесты 503/503. Замер: набор/геометрия зон
  не меняются, но время рождения зон сдвигается (ONDO 15m 815/819; BTC 4h 47/789) — теперь причинно.
  Детали и осознанные решения — `NEGATIVE-KNOWLEDGE.md` (утечка #2).

---

## Трек B — research фильтров

- [x] **B1. Бенчмарк-харнесс фильтров.** ✅ Собран и прогнан (2026-08-14).
  Матрица filter × asset × TF × mode с train/OOS сплитом, издержками, cluster-equal метрикой и
  отдельным long/short gate. Реализация — `tools/research/filterBenchmark.ts` (причинный A1-путь,
  7 фильтров × 5 активов × 3 ТФ × 3 режима, 7 bps, bootstrap CI). Артефакт — `ci-results/filter-benchmark-b1.json`.
  **Результат:** 0/21 ячеек filter×mode с OOS net>0 при CI-low>0 → на текущем information set фильтры
  ВХОДА edge не создают. Вывод и цифры — `NEGATIVE-KNOWLEDGE.md` §8. Открытый хвост: догнать полный
  `npx tsc --noEmit` до коммита (tsx отработал чисто).

- [ ] **B2. Кросс-биржевая устойчивость (дешёвый OOS-прокси).**
  Наблюдение автора (2026-08-13, ONDO 15m, 20k баров): результат зависит от биржи — фитили
  (high/low) триггерят стопы/тейки по-разному, а объём меняет exhaustion/combo и OWN2-триггер.
  Замеры:

  | Биржа      | safe total/stop/tp/part | risk total/stop/tp/part |
  |------------|-------------------------|-------------------------|
  | Bybit      | 88 / 10 / 56 / 22       | 93 / 25 / 48 / 20       |
  | Bybit.P    | 86 / 7 / 55 / 24        | 91 / 19 / 50 / 22       |
  | Binance    | 90 / 12 / 52 / 26       | 98 / 21 / 51 / 26       |
  | Binance.P  | 83 / 9 / 50 / 24        | 89 / 22 / 45 / 22       |

  Идея: гонять конфиг по 3-4 площадкам. Выживает везде → доверия больше; живёт на одной →
  скорее шум/оверфит под микроструктуру площадки. Подтверждённый вывод → перенести каветат в
  `NEGATIVE-KNOWLEDGE.md`. ⚠ Требует, чтобы фетч умел выбирать биржу (сейчас фикс — Binance
  USDT-M futures, `market='futures'`).

- [ ] **B3. Кондиционирование под класс актива («подгонка» в хорошем смысле).**
  Разрешено, НО только с per-asset OOS-валидацией и жёстким лимитом степеней свободы — иначе
  гарантированный оверфит. ⚠ Правила/пороги/группировка активов — решает автор.

---

## Трек D — поиск edge (после B1: вход-фильтры исчерпаны, см. NEGATIVE-KNOWLEDGE §8)
> Гипотеза-разворот: baseline даёт vendor-WR ~0.83 при meanR≈0 → утечка R в **геометрии выхода/риска**,
> а не в селекции входа. Все пункты гоним через тот же харнесс (train/OOS + CI + breadth), пороги —
> ⚠ решает автор. Отрицательный результат по каждому → в `NEGATIVE-KNOWLEDGE.md`.

- [ ] **D1. Exit-benchmark (ПРИОРИТЕТ, в работе).** Тот же допущенный набор стрелок, варьируем ТОЛЬКО выход,
  меряем OOS. Первый threshold-free срез — рекомбинация уже существующих констант `ARROW_MODE_CONFIGS`
  (management dynamic↔static, partial вкл/выкл). Дальше (⚠ новые числа/логика — решает автор): ATR/структурно-
  масштабированный стоп, трейл-к-структуре (last swing/BOS), тейк у дальнего края конверта / противоположного пула.
  Мотив: risk (тесный стоп) стабильно худший, partial-be режет победителей. IMP1 двигал ~0.04R, но лишь ВНУТРИ
  семьи partial/static — структурно другой выход не свипали.
- [ ] **D2. Fade известного отрицательного фактора.** `NEGATIVE-KNOWLEDGE §6`: сигнал помечает ХУДШИЕ входы
  (фактор сильный и отрицательный). Надёжно отрицательный фактор = edge со сменой знака. Зонд: вариант «fade»
  (вход в противоположную сторону) в харнессе, OOS — переворачивается ли знак. Дёшево, высокий upside.
- [x] **D3. Структурный/волновой контекст.** ✅ Прогнан (2026-08-16) — edge НЕТ.
  Раннер `tools/research/structureContextD3.ts` → `ci-results/structure-context-d3.json`. Разметка допущенного пула
  (safe/static/T2, 1307 сделок) структурной позицией на входе (контр-тренд к HTF; premium/discount 4h; дистанция от BOS),
  причинно через готовый `htfContext`. Нейтральные дефолты (HTF=4h, медианный сплит BOS по train=4), НЕ свипались.
  **8/9 корзин не проходят; единственный выживший `pd_premium` (OOS +0.17 CI[+0.04,+0.30]) отрицателен на full/train →
  лид свежего окна, не edge.** Нога импульса — TODO (правило не задано автором, §2.1). Цифры — `NEGATIVE-KNOWLEDGE §8`.
- [~] **D4. Режим РЫНКА (макро вкл/выкл), не режим сигнала.** Внешняя серия состояния (тренд/вола BTC,
  кросс-активный breadth, перцентиль реализованной волы) гейтит ВСЕ сигналы разом. Отличается от B1 — условие
  на экзогенном состоянии, не на признаке самого сигнала. Мотив: long убыточен на всех активах, short ~0 —
  бета-артефакт, который макро-гейт/нейтрализация могут снять.
  **D4a — BTC trend-gate: ОТВЕРГНУТ (2026-08-14).** Нейтральный прокси (BTC-2h close vs SMA600, причинный) не
  разделяет aligned/anti/baseline (все ≈ −0.05, CI через 0), breadth не когерентен. Раннер `tools/research/regimeGateD4.ts`,
  цифры — `NEGATIVE-KNOWLEDGE §8`.
  **D4b — realized-vol gate: directional ОТВЕРГНУТ, но vol-magnitude ЛИД (2026-08-14).** aligned≈anti≈baseline (вола
  не выбирает сторону), НО low-vol значимо отрицателен (low_all −0.135 CI [−0.214, −0.053]), high-vol ≈0 → фильтр
  участия/риска «не торговать в тихом рынке», не edge. Раннер `tools/research/regimeGateD4Vol.ts`, цифры — `NEGATIVE-KNOWLEDGE §8`.
  **D4b-OOS + D4b-temporal (2026-08-14) — D4a/D4b ЗАКРЫТЫ:** low-vol-вето ПОДТВЕРЖДЕНО OOS (low_only OOS −0.214
  [−0.337,−0.084], CI исключает 0 в full и OOS; минусовой в 7/10 полугодий, свежие подряд) — **durable причинный risk-filter**
  «не торговать сигнал в low-vol режиме» (снижает просадку, плюса НЕ даёт). high-vol-плюс **ОТВЕРГНУТ**: знак осциллирует по
  полугодиям, кумулятив ~+0.017 (плоский), OOS +0.174 был концентрацией на BTC + свежем окне 2026-H1 → капкан D-lead.
  Раннеры `tools/research/regimeGateD4bHighVolOos.ts` + `regimeGateD4bTemporal.ts`, цифры — `NEGATIVE-KNOWLEDGE §8`.
  **Итог D4 (trend+vola):** экзогенный режим НЕ выбирает сторону и НЕ создаёт edge; противофаза D-lead не объяснена.
  Непройдено: **D4c** — кросс-активный breadth; **D5** — кросс-секция. НЕ свипать окна волы (selection bias, §2.1).
- [ ] **D5. Кросс-секция / relative-value.** В момент сигнала: long сильнейший сигнал + short корзина/BTC →
  рыночно-нейтрально, вычитает бету, топящую лонги. Другой источник edge (относительная сила), а не ещё фильтр.
- [ ] **D6. Новая информация (план Б, если D1–D5 упрутся).** `NEGATIVE-KNOWLEDGE §3`: exact-bar, вероятно,
  зависит от intrabar/HTF-stateful/OI/funding, которых нет в экспорте. Инфраструктурно дорого → последний рычаг.

---

## Трек C — UI / производительность

- [ ] **C1. Убрать Decision Lab из интерфейса.**
- [ ] **C2. Оптимизация загрузки графика** (кэш свечей уже есть в server.ts; смотреть пагинацию /
  ленивую подгрузку слоёв / размер payload / тяжёлые слои по требованию).
- [ ] **C3. Разгрузить перегруженный интерфейс** (конкретный список «что лишнее / что переставить» —
  собрать отдельно с автором).

---

## Открытые решения автора
- **Paper-forward H1 Risk — ОТЛОЖЕН** (решение 2026-08-13). Не приоритет сейчас.
- Порядок/параллельность: автор предпочитает вести A2 + трек C параллельно с research (B).

## Красный список
Напоминание — в `docs/HANDOFF.md` (vendor-выгрузки, forward-аудиты, калибровочные константы Apex/POI
не трогать без явного запроса).
