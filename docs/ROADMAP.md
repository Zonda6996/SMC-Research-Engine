# ROADMAP — живой план (только открытые задачи)

> **Что это:** план активной работы. **Только открытое** — закрытые треки (RE1–RE25, S1–S5,
> A/B/D/E, funding-линии, OWN2-корпуса) лежат в `docs/archive/ROADMAP-2026-08-23-full.md`
> (снимок, только чтение); выводы из них дистиллированы в `docs/strategies/zonda-reversal.md`
> и `docs/NEGATIVE-KNOWLEDGE.md`.
> **Как вести:** статус в чекбоксах (`[ ]` → `[x]`), краткий итог рядом. Правило гигиены:
> трек закрылся → в архив в тот же день.
> **Как часто меняется:** часто. Читать после `HANDOFF.md`. Язык — русский.

---

## Трек F — Doppler (D6 Cascade Reversion) — активная линия

> Спека: `docs/strategies/doppler.md` · статус и цифры: `docs/HANDOFF.md` · отвергнутое:
> `docs/NEGATIVE-KNOWLEDGE.md` (раздел D6).
> Сигнал: каскадный делевериджинг (ΔOI_8h≤−15% И ΔP_8h≤−3%) → LONG next-open, структурный стоп,
> таймаут 72ч.

- [x] GO ×2 независимых репликации (extension H24 + mgmt, лучшая рука H72-stopStruct) —
      протоколы `ci-results/d6-*`
- [x] d6-tp: фиксированные тейки по R — `KILL`; контроль не реплицировался на третьей вселенной
      (2026-08-23); MFE-справка: до ≥1.5R доходит 33.4% сделок
- [x] **Census-карта А — мажоры, 12 символов** ✅ 2026-08-23 (`ci-results/d6-census-majors.*`):
      все 10 ячеек сетки в плюс, монотонно с глубиной; автор утвердил правило **−15/−5**.
- [x] **Census Б** ✅ 2026-08-23 (`ci-results/d6-census-b.*`): 16 мид-капов с историей ≥20k баров
      (55 скипнуто молодых). Правило −15/−5: **+1.33% (WR 41%, N=220)** — в плюс, но слабее мажоров
      (+3.11%); глубже −15 на Б качество падает. События во всех 16 символах.
- [x] **Prereg «матрёшка» рук** ✅ 2026-08-23, терминальный reveal на мажорах
      (`ci-results/d6-partial-*`): **контроль C-H72 — лучшая рука** (+3.11% [+0.38;+5.60],
      breadth 10/12); частички 50% @1R/1.5R ± безубыток НЕ улучшают (KILL/слабее контроля).
      Вывод линии: хвост платит за серию — не обрезать. ⚠ Правило in-sample (census),
      подтверждение — Б/форвард. Фулл-тейки по R не переоткрывать (KILL d6-tp).
- [x] **Census мульти-ТФ (диагностика)** ✅ 2026-08-23 (`ci-results/d6-census-multitf.*`):
      все 18 ячеек (6 ТФ × 3 режима) в плюс; сладкая зона 30м–1ч; SAFE ≥ STANDARD ≥ RISK на всех ТФ;
      5м/15м — цифры огромные, но N=1–74 и жёсткая кластеризация (не вывод, только направление).
- [x] **Доп. диагностики (2026-08-24, решение автора):** (а) SHORT-зеркало — **мусор** (WR 18–21%,
      net −0.29…+0.03%, `ci-results/d6-census-short.*`) — согласуется с TIDAL «шорты после пампов
      токсичны», линию не развивать; (б) LIMIT-вход у рынка — ничего не меняет (заполняется
      мгновенно по той же цене, `ci-results/d6-limit-entry.*`); более глубокие уровни лимиток —
      только по отдельному решению автора; (в) **Apex-scalp 5м/15м — направление подтверждено**:
      вся прибыль в классе «глубоко в зоне» (`ci-results/d6-apex-scalp.*`: 15м in-zone-deep
      +2.7…+7.8% при WR 59–71% против слабого/отрицательного вне зоны) → кандидат на отдельный
      prereg после уточнения дизайна с автором.
- [x] **Doppler Multi-TF prereg + reveal** ✅ 2026-08-24 (`ci-results/d6-multitf-*`, prereg `df981df1…`,
      манифест `8e172fc4…`): окно жёстко 8ч на всех ТФ (дизайн автора «масштабируемость как в GGI»).
      **Линейный вердикт `INCONCLUSIVE DATA`** по замороженному гейту (2ч/4ч STANDARD <100 событий);
      внутри: 5m|STANDARD **GO** (+2.72% [+0.39;+5.06], N=154), 15m|STANDARD **GO** (+2.06%
      [+0.03;+3.99], N=149); 30m/1h KILL на волоске (CI-low −0.01/−0.09); SAFE-руки <100 (по гейту
      INCONCLUSIVE). **Ключевая находка:** 1h с reclaim-выходом +2.48% против чистого таймаута +3.11%
      (d6-partial) — reclaim срезает ~0.6п.п./сделку → аргумент за таймаут-выход в форварде (решение автора).
- [x] **Scaled-timeout диагностика** ✅ (`ci-results/d6-scaled-timeout.*`): 72ч выигрывает на всех
      ТФ и в обоих режимах (короткие таймауты стабильно хуже) — ритм сделки задаёт тесный стоп, не время.
- [x] **Fixed-window карта** ✅ (`ci-results/d6-fixed-window.*`): окно 8ч на всех ТФ — события
      выравниваются (5м: 154, не 10); интуиция автора подтверждена.
- [x] **Apex-scalp prereg + reveal** ✅ 2026-08-24 (`ci-results/d6-apex-{preregistration,results}.*`,
      prereg `2d988479…`): фильтр «глубоко в зоне Apex» на каскадных входах, 6 ТФ × 2 режима,
      998 событий (deep 472). **Вердикт по замороженному гейту: `KILL`** — pooled Δ deep−rest
      +2.65% при CI95 [−0.84; +5.12] (дневная кластеризация раздувает дисперсию). Направление
      стабильно: deep > rest во ВСЕХ 12 руках (deep WR до 84–100% на 2ч/4h), но фильтр НЕ
      подтверждён — не использовать в торговле; перепроверка только на свежих данных.
- [x] **Apex-варианты на вселенной Б** ✅ 2026-08-24 (`ci-results/d6-apex-b-*`, prereg
      `d70ac21b…`, манифест `e877614c…`): три класса (deep/inner/mean) на 16 мид-капах,
      324 события — **все три `KILL`** (CI широкие из-за кластеризации), НО свежевселенная
      репликация направления: класс всегда в плюсе (+1.79…+3.44%), комплемент всегда В МИНУСЕ
      (−0.37…−1.59%) — зона отделяет прибыльные каскады от убыточных на двух независимых
      вселенных; для торговли не подтверждено, для будущих prereg на свежих данных — задел.
- [x] **Форвард → чистый H72-таймаут** ✅ 2026-08-24 (решение автора по данным d6-multitf:
      reclaim срезал ~0.6 п.п./сделку). Журнал продолжён, формат отчёта без изменений.
- [ ] **Решения автора по картам (ночь 2026-08-23):** (а) терминальный reveal подтверждения
      −15/−5 на Б? (б) какая ТФ-рука идёт в prereg (30м/1ч кандидаты; 5м/15м — сначала
      скальп-census с зонами Apex)? (в) перевод форвард-журнала сделан: SAFE/STANDARD/RISK пишутся
      параллельно.
- [x] **D6 × TIDAL portfolio feasibility** ✅ 2026-08-24 (`ci-results/d6-tidal-portfolio.*`):
      корреляция ног в активные дни **0.084** (почти независимы); комбо 50/50 daily: +1278% против
      carry +763% / Doppler +928%; Sharpe комбо 1.34 < carry 1.44 → наивный 50/50 НЕ оптимальная
      структура (комковатость Doppler размывает carry); правильная структура = раздельные книги
      (carry база + Doppler сателлит). Carry-форвард TIDAL запущен 2026-08-24, дневные серии
      SELECT выгружены.
- [ ] **Мульти-ТФ пачка:** 5m/15m/30m/1h/2h/4h; окна пропорциональны (8 баров соответствующего ТФ),
      пороги события НЕ трогать. Переиспользование символов, сожжённых на 1h, разрешено автором;
      каждая ТФ-рука = отдельная preregistration. ⚠ 5m/15m дополнительно тестировать как
      скальп-подтверждение локальных зон Apex (идея автора) — отдельный census.
- [ ] **Paper-forward: накопление.** ~10–15 сигналов/мес на 37 символах; MFE-в-R пишется по каждой
      сделке; паритет live-OI vs архивы сверять по мере публикации архивов; промежуточная сверка
      профиля после ~10–15 сделок. + Решение по автозапуску раз в сутки.
- [ ] **Детектор Doppler → `src/core`.** Один модуль вместо трёх копий в раннерах (mgmt/tp/forward)
      + тест паритета. Сделать ПЕРЕД слоем визуализатора.
- [ ] **Индикатор Doppler в визуализаторе:** маркеры (бар-сигнал, вход, стоп, цель) + мини-таблица
      статистики по активу (N/WR/PF/средняя/MFE). После оптимизации трека C.

## Трек C — UI / производительность визуализатора (давняя претензия автора)

- [ ] **C1. Убрать Decision Lab из интерфейса.**
- [ ] **C2. Оптимизация загрузки графика** (сейчас 20к свечей грузятся 30–60с): canvas/даунсэмплинг
      при отдалении, ленивая подгрузка слоёв, вынос движка в web worker; кэш свечей в server.ts уже есть.
- [ ] **C3. Разгрузить перегруженный интерфейс** (список «что лишнее / что переставить» — собрать
      отдельно с автором).

## Хозяйство (остаток)

- [ ] Автозапуск paper-forward (Планировщик задач Windows, раз в сутки) — после «го» автора.
- [ ] Концепт-док «два проекта» (SMC-Research-Engine vs crypto-futures-signals): пока держать
      раздельно — тот проект = строгий статистический станок (Python, SPA/Holm/DSR), без OI-данных;
      граница описана в его HANDOFF. Возвращаться после решения популяции Doppler и их FINAL-001.
- [ ] Pruning старых `ci-results/` — ТОЛЬКО по явному списку автора.

## Принципы (из AGENTS.md)

- Правила анализа не выдумываем (§2.1). Пороги/условия значимости — только по явному решению автора.
- Найденное не чиним молча (§2.2): сначала показать/обсудить, потом менять.
- Дисциплина проверки: гипотеза → отложенное окно (OOS), preregistration до данных; отчёт — по §7 AGENTS.
